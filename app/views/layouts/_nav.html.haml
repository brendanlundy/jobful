%nav.top-bar
  %section.top-bar-section
    %ul.right
      %li.has-dropdown
        -if user_signed_in?
          = link_to current_user.email, user_job_applications_path(current_user)
          %ul.dropdown
            %li.active
              = link_to "logout", destroy_user_session_path
        -else
          / = link_to "Join", new_user_registration_path
          %ul.dropdown
            = link_to "Login", new_user_session_path
